{
  "name": "jumpiteasy-root",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "start:backend": "cd backend && node index.js",
    "start:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "jumpiteasy-root": "file:"
  }
}
